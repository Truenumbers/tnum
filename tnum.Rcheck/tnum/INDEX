tnum.authorize          Connect and authenticate to Truenumbers server
tnum.createSpace        Create new numberspace
tnum.decodeNumericVectorString
                        Return numeric vector from string
                        "vector(1.23,34.5....)"
tnum.deleteByQuery      Delete tnums specified by a query
tnum.getAttrFromList    Title
tnum.getDBPathList      Get phrase taxonomies
tnum.getDatabasePhraseList
                        Get phrase taxonomies
tnum.graphPathList      Make and render a graph from a list of phrase
                        paths
tnum.graphTnumList      Make full tnum graph from tnum.query return
                        data frame
tnum.makeNumericVectorString
                        Create a tnum vector value string
                        "vector(23,-34.02...)" from an R vector or list
tnum.makeObject         make a tnum object from numeric values in a
                        tnum data frame
tnum.makePhraseGraphFromPathList
                        Get a DiagrammeR tree for rendering, from a
                        list of SRD paths
tnum.makeTnumJson       Create a JSON truenumber from parts
tnum.makeTnumPhraseGraph
                        Make full tnum graph from tnum.query return
                        data frame
tnum.objectsToDf        make data frame from list of tnum objects
tnum.plotGraph          Plot DiagrammeR graph
tnum.postFromLists      Create many truenumbers from lists of parts
tnum.postObjects        post a list or vector of tnum objects
tnum.query              Query the truenumber DB
tnum.queryResultToObjects
                        Convert tnum query result to an object list
tnum.tagByQuery         Tag tnums specified by a query
